# Post-hoc analysis
# Tukey multiple pairwise-comparisons
# Tukey HSD (Tukey Honest Significant Differences)
TukeyHSD(result_anova);
# Post Analysis
# 1. Any pattern between Residual and Fitted value?
# 2. Residual is normally distributed or not?
plot(result_anova)
MyData5 <- read_xlsx("./Data/parametric_data.xlsx", sheet = "Sheet5")
MyData5$Instructor = factor(MyData5$Instructor,
levels=unique(MyData5$Instructor))
MyData5$Supplement = factor(MyData5$Supplement,
levels=unique(MyData5$Supplement))
# Use Summarize to check if cell sizes are balanced
# We can use the n or nvalid columns in the Summarize output to check if cell sizes are balanced (if each combination of treatments have the same number of observations).
library(rcompanion)
Mean_conf2 <- groupwiseMean(Sodium ~ Instructor, data = MyData5,
conf = 0.95, digits = 3)
Mean_conf2
Mean_conf3 <- groupwiseMean(Sodium ~ Supplement, data = MyData5,
conf = 0.95, digits = 3)
Mean_conf3
ggplot(Mean_conf2,aes(x = Instructor,y = Mean)) +
geom_errorbar(aes(ymin = Trad.lower, ymax = Trad.upper), width = 0.1, size  = 0.5)+
geom_point(shape = 15, size  = 4) + #point of mean
theme_bw() +
theme(axis.title = element_text(face = "bold")) +
ylab("Mean sodium, mg")
ggplot(Mean_conf3,aes(x = Supplement,y = Mean)) +
geom_errorbar(aes(ymin = Trad.lower, ymax = Trad.upper), width = 0.1, size  = 0.5)+
geom_point(shape = 15, size  = 4) + #point of mean
theme_bw() +
theme(axis.title = element_text(face = "bold")) +
ylab("Mean sodium, mg")
library(FSA)
Summarize(Sodium ~ Supplement + Instructor,
data = MyData5,
digits = 3)
# Define linear model
# Note that the interaction of Instruction and Supplement is indicated in the model definition by Instructor:Supplement.
# Histogram of residuals
# Conduct analysis of variance
result_anova2 <- aov(Sodium ~ Instructor + Supplement, data = MyData5)
summary(result_anova2)
result_anova3 <- aov(Sodium ~ Instructor , data = MyData5)
summary(result_anova2)
# Another method (run ANOVA directly)
# Tukey multiple pairwise-comparisons
# Tukey HSD (Tukey Honest Significant Differences
TukeyHSD(result_anova2)
# Post Analysis
# 1. Any pattern between Residual and Fitted value?
# 2. Residual is normally distributed or not?
plot(result_anova3)
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(FSA)){install.packages("FSA")}
if(!require(BSDA)){install.packages("BSDA")}
if(!require(nonpar)){install.packages("nonpar")}
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(FSA)){install.packages("FSA")}
if(!require(BSDA)){install.packages("BSDA")}
if(!require(nonpar)){install.packages("nonpar")}
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(FSA)){install.packages("FSA")}
if(!require(BSDA)){install.packages("BSDA")}
if(!require(nonpar)){install.packages("nonpar")}
Height = c(110, 132, 137, 139, 140, 142, 142, 145)
names(Height) = letters[1:8]
Height
rank(Height)
LikertData1 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet1")
# Create a new variable which is the likert scores as an ordered factor
LikertData1$Likert.f = factor(LikertData1$Likert, ordered = TRUE)
LikertData1 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet1")
# Create a new variable which is the likert scores as an ordered factor
LikertData1$Likert.f = factor(LikertData1$Likert, ordered = TRUE)
# Check the data frame
summary(LikertData1)
# Cross Tabulation
XT <- xtabs( ~ Speaker + Likert.f,
data = LikertData1)
XT
prop.table(XT, margin = 1)
# Summarize data treating Likert scores as numeric
library(FSA)
Summarize(Likert ~ Speaker,
data = LikertData1,
digits = 3)
# Bar plot
# Sign test with the BSDA package
# Note that md = 3 indicates the default value to compare to.
# H1: Median > 3
# Sign test with the DescTools package
# Note that mu = 3 indicates the default value to compare to.
# H1: Median > 3
# Conclusion
View(LikertData1)
View(LikertData1)
LikertData1 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet1")
# Create a new variable which is the likert scores as an ordered factor
LikertData1$Likert_f = factor(LikertData1$Likert, ordered = TRUE)
# Check the data frame
summary(LikertData1)
# Check the data frame
summary(LikertData1)
XT <- xtabs( ~ Speaker + Likert_f,
data = LikertData1)
XT
prop.table(XT, margin = 1)
XT <- xtabs( ~ Speaker + Likert_f,
data = LikertData1)
XT
prop.table(XT, margin = 1)
library(FSA)
Summarize(Likert ~ Speaker,
data = LikertData1,
digits = 3)
# Bar plot
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar();
# Bar plot
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar()+
scale_x_discrete(drop = false);
# Bar plot
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar()+
scale_x_discrete(drop = FALSE);
t_f))+
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar()+
scale_x_discrete(drop = FALSE);
# Create a new variable which is the likert scores as an ordered factor
LikertData1$Likert_f = factor(LikertData1$Likert, level = 1:5,ordered = TRUE)
# Bar plot
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar()+
scale_x_discrete(drop = FALSE);
# Sign test with the BSDA package
# Note that md = 3 indicates the default value to compare to.
# H1: Median > 3
SIGN.test(LikertData1$Likert_f,
m = 3,
alternative = "greater");
# Sign test with the BSDA package
# Note that md = 3 indicates the default value to compare to.
# H1: Median > 3
SIGN.test(LikertData1$Likert_f,
m = 3,
alternative = "greater",
conf.level = 0.95);
LikertData1 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet1")
# Create a new variable which is the likert scores as an ordered factor
LikertData1$Likert_f = factor(LikertData1$Likert, level = 1:5,ordered = TRUE)
# Check the data frame
summary(LikertData1)
# Cross Tabulation
XT <- xtabs( ~ Speaker + Likert_f,
data = LikertData1)
XT
prop.table(XT, margin = 1)
# Summarize data treating Likert scores as numeric
library(FSA)
Summarize(Likert ~ Speaker,
data = LikertData1,
digits = 3)
# Bar plot
ggplot(LikertData1, aes(x = Likert_f))+
geom_bar()+
scale_x_discrete(drop = FALSE);
# Sign test with the BSDA package
# Note that md = 3 indicates the default value to compare to.
# H1: Median > 3
SIGN.test(LikertData1$Likert_f,
m = 3,
alternative = "greater",
conf.level = 0.95);
# Sign test with the BSDA package
# Note that md = 3 indicates the default value to compare to.
# H1: Median > 3
SIGN.test(LikertData1$Likert,
m = 3,
alternative = "greater",
conf.level = 0.95);
LikertData2 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet2")
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(ggmosaic)){install.packages("ggmosaic")}
LikertData2 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet2")
LikertData2 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet2")
View(LikertData2)
# Order levels of the factor, otherwise R will alphabetize them.
LikertData2$Speaker = factor(LikertData2$Speaker)
# Create a new variable which is the Likert scores as an ordered factor
LikertData2$Likert.f = factor(LikertData2$Likert,
ordered = TRUE)
# Order levels of the factor, otherwise R will alphabetize them.
LikertData2$Speaker = factor(LikertData2$Speaker, levels = unique(LikertData2$Speaker))
# Create a new variable which is the Likert scores as an ordered factor
LikertData2$Likert.f = factor(LikertData2$Likert,
ordered = TRUE)
# Cross Tabulation
XT2 <- xtabs( ~ Speaker + Likert.f,
data = LikertData2)
XT2
View(LikertData2)
Summarize(Likert ~ Speaker,
data = LikertData2,
digits = 3)
prop.table(XT2, margin = 1)
Summarize(Likert ~ Speaker,
data = LikertData2,
digits = 3)
# Histograms of each group
ggplot(LikertData2, aes(x = Speaker, y = Likert)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Speaker, y = Likert)) +
geom_histogram(bin = 5);
# Histograms of each group
ggplot(LikertData2, aes(x = LikertData2$Speaker, y = LikertData2$Likert)) +
geom_histogram(bin = 5);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(bin = 5);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Speaker, fill = Likert)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Likert.f, fill = Speaker)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram();
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = count);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count");
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(~ speaker);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(speaker ~);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(~ Speaker);
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(~ Speaker) +
margin(legend = "none");
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(~ Speaker)
# Exact median test with "coin" package
# H1: median_Piglet != median_Pooh
library(coin)
#Density plot
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) + geom_density()
#Density plot
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) + geom_density() + facet_grid(~ Speaker)
shapiro.test(LikertData2$Speaker[1:10])
shapiro.test(LikertData2$Likert[1:10])
shapiro.test(LikertData2$Likert[11:20])
shapiro.test(LikertData2$Likert[1:10])
shapiro.test(LikertData2$Likert[11:20])
mediantest(Likert ~ Speaker,
data = LikertData2,
med = 0,
alternative = "greater")
mediantest(Likert ~ Speaker,
data = LikertData2,
alternative = "greater")
# Use "nonpar" package
library(nonpar)
mediantest(Likert ~ Speaker,
data = LikertData2,
alternative = "greater")
mediantest(Likert ~ Speaker,
data = LikertData2,
m = 0,
alternative = "greater")
median_test(Likert ~ Speaker,
data = LikertData2,
m = 0,
alternative = "greater")
median_test(Likert ~ Speaker,
data = LikertData2,
m = 0,
alternative = "greater")
median_test(Likert ~ Speaker,
data = LikertData2,
alternative = "greater")
median_test(x = Likert, y = Speaker,
data = LikertData2,
alternative = "greater")
median_test(x = LikertData2$Likert, y = LikertData2$Speaker,
data = LikertData2,
alternative = "greater")
median_test(Likert ~ Speaker,
data = LikertData2,
alternative = "greater")
knitr::opts_chunk$set(echo = TRUE)
if(!require(mlr)){install.packages("mlr", dependencies = TRUE)}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(mclust)){install.packages("mclust")}
knitr::opts_chunk$set(echo = TRUE)
if(!require(mlr)){install.packages("mlr", dependencies = TRUE)}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(mclust)){install.packages("mclust")}
data("diabetes", package = "mclust");
force(diabetes)
force(diabetes)
View(diabetes)
View(diabetes)
diabetes <- as_tibble(diabetes);
data("diabetes", package = "mclust");
diabetes <- data("diabetes", package = "mclust");
diabetes
diabetes <- as_tibble(diabetes);
diabetes
diabetes <- data("diabetes", package = "mclust");
knitr::opts_chunk$set(echo = TRUE)
if(!require(mlr)){install.packages("mlr", dependencies = TRUE)}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(mclust)){install.packages("mclust")}
diabetes <- data("diabetes", package = "mclust");
data("diabetes", package = "mclust");
diabetes
data("diabetes", package = "mclust");
diabetes
diabetes
diabetes <- as_tibble(diabetes);
diabetes <- as_tibble(diabetes);
diabetes
diabetes |> ggplot(aes(x = glucose, y = insulin)) + geom_point();
diabetes |> ggplot(aes(x = glucose, y = insulin, color = color)) + geom_point();
diabetes |> ggplot(aes(x = glucose, y = insulin, color = class)) + geom_point();
diabetes |> ggplot(aes(x = glucose, y = insulin, color = class)) +
geom_point() +
facet_grid(legend = "bottom");
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(FSA)){install.packages("FSA")}
if(!require(BSDA)){install.packages("BSDA")}
if(!require(nonpar)){install.packages("nonpar")}
LikertData2 <- read_xlsx("./Data/nonparametric_data.xlsx", sheet = "Sheet2")
# Order levels of the factor, otherwise R will alphabetize them.
LikertData2$Speaker = factor(LikertData2$Speaker, levels = unique(LikertData2$Speaker))
# Create a new variable which is the Likert scores as an ordered factor
LikertData2$Likert.f = factor(LikertData2$Likert,
ordered = TRUE)
# Cross Tabulation
XT2 <- xtabs( ~ Speaker + Likert.f,
data = LikertData2)
XT2
prop.table(XT2, margin = 1)
# Summarize data treating Likert scores as numeric
library(FSA)
Summarize(Likert ~ Speaker,
data = LikertData2,
digits = 3)
# Histograms of each group
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_histogram(stat = "count") +
facet_grid(~ Speaker)
#Density plot
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_density() +
facet_grid(~ Speaker);
#Shapilo-Wilk test
shapiro.test(LikertData2$Likert[1:10])
shapiro.test(LikertData2$Likert[11:20])
#Both less than 0.05, so we have enough evidence for reject Null Hypothesis, then LikertData2 is not normal Dist
# Exact median test with "coin" package
# H1: median_Piglet > median_Pooh
library(coin)
median_test(Likert ~ Speaker,
data = LikertData2,
alternative = "greater")
# Use "nonpar" package
library(nonpar)
# Conclusion
#Density plot
ggplot(LikertData2, aes(x = Likert, fill = Speaker)) +
geom_density() +
facet_grid(~ Speaker);
diabetes |> ggplot(aes(x = glucose, y = insulin, color = class)) +
geom_point() +
theme(legend.position = "bottom");
diabetes |> ggplot(aes(x = glucose, y = insulin, color = class)) +
geom_point() +
theme(legend.position = "bottom")
diabetes |> ggplot(aes(x = glucose, y = sspg, color = class)) +
geom_point() +
theme(legend.position = "bottom")
diabetes |> ggplot(aes(x = sspg, y = insulin, color = class)) +
geom_point() +
theme(legend.position = "bottom")
makeClassifTask(data = diabetes, target = "class")
diabetes_task <- makeClassifTask(data = diabetes, target = "class")
diabetes_task
makeLearner("classif.knn", par.vals = list(k = 2))
myKnn <- makeLearner("classif.knn", par.vals = list(k = 2))
my_knn
myKnn
# learner before task
knn_nodels <- train(learner = myKnn, task =  diabetes_task)
# learner before task
knn_nodels <- train(learner = myKnn, task =  diabetes_task)
# learner before task
knn_model <- train(learner = myKnn, task =  diabetes_task)
knn_model
predict(knn_model, newdata = diabetes);
knn_predict <- predict(knn_model, newdata = diabetes);
knn_predict
View(knn_predict)
View(knn_predict)
View(knn_predict)
knn_predict$data
performance(knn_predict);
performance(knn_predict, measures = list(mmce, acc));
makeResample = (method = "Holdout", split  = 2 / 3, stratify = T);
makeResampleDesc(method = "Holdout", split  = 2 / 3, stratify = T);
holdout <- makeResampleDesc(method = "Holdout", split  = 2 / 3, stratify = T);
holdout <- makeResampleDesc(method = "Holdout", split  = 2 / 3, stratify = T);
holdout <- makeResampleDesc(method = "Holdout", split  = 2 / 3, stratify = T);
holdout <- makeResampleDesc(method = "Holdout", split  = 2 / 3, stratify = T);
resample(learner = myKnn,
task = diabetes_task,
resampling = holdout,
measure  = list(mmce, acc));
hold_cv <- resample(learner = myKnn,
task = diabetes_task,
resampling = holdout,
measure  = list(mmce, acc));
holdout_cv$pred$data
holdout_cv <- resample(learner = myKnn,
task = diabetes_task,
resampling = holdout,
measure  = list(mmce, acc));
holdout_cv$pred$data
calculateConfusionMatrix(holdout_cv$pred, relative = T)
k4 <- makeResampleDesc(method = "RepCV", folds = 10,rep = 50, stratify =T);
k4_CV <- resample(learner = myKnn,
task = diabetes_task,
resampling = k4,
measure  = list(mmce, acc));
k4_CV$pred$data
knitr::opts_chunk$set(echo = TRUE)
# Call required packages, ex. "readxl", "tidyverse"
if(!require(readxl)){install.packages("readxl")}
if(!require(tidyverse)){install.packages("tidyverse")}
if(!require(rcompanion)){install.packages("rcompanion")}
if(!require(DescTools)){install.packages("DescTools")}
if(!require(Hmisc)){install.packages("Hmisc")}
if(!require(corrplot)){install.packages("corrplot")}
if(!require(formatR)){install.packages("formatR")}
if(!require(FSA)){install.packages("FSA")}
if(!require(BSDA)){install.packages("BSDA")}
if(!require(nonpar)){install.packages("nonpar")}
